# Normalize text files across platforms.
* text=auto

# Keep PowerShell scripts with Windows line endings.
*.ps1 text eol=crlf

# Keep numerical text tables and CSV data with stable Unix line endings.
data/*.txt text eol=lf
*.csv text eol=lf

# Treat NumPy archives as binary files.
*.npz binary
